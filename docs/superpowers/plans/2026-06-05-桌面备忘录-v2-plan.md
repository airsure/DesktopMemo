# 桌面备忘录 v2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将单文件桌面壁纸备忘录重构为基于 PyQt5 的桌面应用——桌面右侧显示任务 overlay，系统托盘隐藏编辑界面，支持拖拽排序和三套主题切换。

**Architecture:** 三个 PyQt5 窗口（桌面显示面板 + 编辑主界面 + 系统托盘）围绕一个 JSON 数据文件协作。编辑界面修改数据后通过 Qt 信号刷新显示面板。显示面板鼠标穿透、置顶、无边框。

**Tech Stack:** Python 3.x, PyQt5, JSON 文件存储

---

## 文件职责

| 文件 | 职责 |
|------|------|
| `main.py` | 入口，创建 QApplication，启动 App |
| `app.py` | 应用主控：托盘图标、窗口生命周期、快捷键、信号连接 |
| `overlay.py` | 桌面显示面板：鼠标穿透、置顶、渲染任务列表 |
| `editor.py` | 编辑主界面：输入栏、任务列表、拖拽排序、右键菜单 |
| `data.py` | 数据层：Task 数据类、DataStore JSON 读写 |
| `theme.py` | 三套主题定义（暗夜/明亮/毛玻璃），颜色常量 |
| `requirements.txt` | 依赖声明 |

---

### Task 1: 项目初始化和依赖

**Files:**
- Create: `requirements.txt`

- [ ] **Step 1: 创建 requirements.txt**

```text
PyQt5>=5.15.0
```

- [ ] **Step 2: 安装依赖**

```bash
pip install -r requirements.txt
```

- [ ] **Step 3: 提交**

```bash
git add requirements.txt
git commit -m "chore: 添加 PyQt5 依赖"
```

---

### Task 2: 数据层 data.py

**Files:**
- Create: `data.py`

- [ ] **Step 1: 编写 Task 数据类和 DataStore**

```python
"""数据层：Task 数据类和 JSON 文件读写."""
from __future__ import annotations

import json
import os
import uuid
from dataclasses import dataclass, field
from typing import Optional


CATEGORIES = ["now", "fire", "follow", "done", "memo"]

CATEGORY_LABELS = {
    "now": "进行中",
    "fire": "紧急",
    "follow": "跟进",
    "done": "已完成",
    "memo": "备忘",
}


@dataclass
class Task:
    """单条任务."""
    text: str
    category: str = "now"
    done: bool = False
    sort: int = 0
    id: str = field(default_factory=lambda: uuid.uuid4().hex[:8])

    def to_dict(self) -> dict:
        return {
            "id": self.id,
            "text": self.text,
            "category": self.category,
            "done": self.done,
            "sort": self.sort,
        }

    @classmethod
    def from_dict(cls, d: dict) -> Task:
        return cls(
            id=d.get("id", uuid.uuid4().hex[:8]),
            text=d["text"],
            category=d.get("category", "now"),
            done=d.get("done", False),
            sort=d.get("sort", 0),
        )


class DataStore:
    """JSON 文件数据存储."""

    def __init__(self, filepath: Optional[str] = None):
        if filepath is None:
            filepath = os.path.join(os.path.expanduser("~"), "备忘.json")
        self._filepath = filepath
        self._tasks: list[Task] = []
        self._theme: str = "dark"
        self._load()

    @property
    def tasks(self) -> list[Task]:
        return self._tasks

    @property
    def theme(self) -> str:
        return self._theme

    @theme.setter
    def theme(self, value: str):
        self._theme = value
        self._save()

    def _load(self):
        if not os.path.exists(self._filepath):
            self._save()
            return
        with open(self._filepath, "r", encoding="utf-8") as f:
            data = json.load(f)
        self._theme = data.get("theme", "dark")
        self._tasks = [Task.from_dict(t) for t in data.get("tasks", [])]
        self._renumber()

    def _save(self):
        data = {
            "version": 1,
            "theme": self._theme,
            "tasks": [t.to_dict() for t in self._tasks],
        }
        with open(self._filepath, "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)

    def add_task(self, text: str, category: str = "now") -> Task:
        task = Task(text=text, category=category, sort=len(self._tasks))
        self._tasks.append(task)
        self._save()
        return task

    def update_task(self, task_id: str, **kwargs):
        for t in self._tasks:
            if t.id == task_id:
                for k, v in kwargs.items():
                    if hasattr(t, k):
                        setattr(t, k, v)
                self._save()
                return

    def delete_task(self, task_id: str):
        self._tasks = [t for t in self._tasks if t.id != task_id]
        self._renumber()
        self._save()

    def reorder(self, task_ids: list[str]):
        """按传入的 ID 顺序重新编号 sort 字段."""
        order_map = {tid: i for i, tid in enumerate(task_ids)}
        for t in self._tasks:
            if t.id in order_map:
                t.sort = order_map[t.id]
        self._save()

    def get_active_tasks(self) -> list[Task]:
        """获取未完成的任务，按 sort 排序."""
        return sorted(
            [t for t in self._tasks if not t.done],
            key=lambda t: t.sort,
        )

    def _renumber(self):
        for i, t in enumerate(self._tasks):
            t.sort = i
```

- [ ] **Step 2: 验证数据层能正常读写**

```bash
python -c "
from data import DataStore, Task
import os, tempfile

# 使用临时文件测试
tmp = os.path.join(tempfile.gettempdir(), 'test_memo.json')
ds = DataStore(tmp)
assert len(ds.tasks) == 0

t = ds.add_task('测试任务', 'now')
assert t.id is not None
assert len(ds.tasks) == 1

ds.update_task(t.id, done=True)
assert ds.tasks[0].done is True

ds.delete_task(t.id)
assert len(ds.tasks) == 0

os.remove(tmp)
print('数据层测试通过')
"
```

- [ ] **Step 3: 提交**

```bash
git add data.py
git commit -m "feat: 添加数据层 Task/DataStore"
```

---

### Task 3: 主题定义 theme.py

**Files:**
- Create: `theme.py`

- [ ] **Step 1: 编写主题定义**

```python
"""主题定义：暗夜 / 明亮 / 毛玻璃."""
from __future__ import annotations


CATEGORY_COLORS = {
    "now": "#ff4757",
    "fire": "#ff6348",
    "follow": "#1e90ff",
    "done": "#2ed573",
    "memo": "#ff6b81",
}


class Theme:
    """单个主题的样式配置."""

    def __init__(self, name: str, overlay_bg: str, editor_bg: str,
                 editor_text: str, editor_border: str, row_hover: str,
                 input_bg: str, input_text: str, text_primary: str):
        self.name = name
        self.overlay_bg = overlay_bg
        self.editor_bg = editor_bg
        self.editor_text = editor_text
        self.editor_border = editor_border
        self.row_hover = row_hover
        self.input_bg = input_bg
        self.input_text = input_text
        self.text_primary = text_primary


THEMES = {
    "dark": Theme(
        name="暗夜",
        overlay_bg="rgba(20, 20, 35, 0.88)",
        editor_bg="#2b2b2b",
        editor_text="#cccccc",
        editor_border="#444444",
        row_hover="#3a3a3a",
        input_bg="#444444",
        input_text="#ffffff",
        text_primary="#dddddd",
    ),
    "light": Theme(
        name="明亮",
        overlay_bg="rgba(255, 255, 255, 0.90)",
        editor_bg="#f5f5f5",
        editor_text="#333333",
        editor_border="#dddddd",
        row_hover="#e8e8e8",
        input_bg="#ffffff",
        input_text="#333333",
        text_primary="#333333",
    ),
    "glass": Theme(
        name="毛玻璃",
        overlay_bg="rgba(255, 255, 255, 0.10)",
        editor_bg="rgba(30, 30, 30, 0.85)",
        editor_text="#dddddd",
        editor_border="rgba(255, 255, 255, 0.10)",
        row_hover="rgba(255, 255, 255, 0.08)",
        input_bg="rgba(255, 255, 255, 0.10)",
        input_text="#ffffff",
        text_primary="#ffffff",
    ),
}


def get_theme(key: str) -> Theme:
    return THEMES.get(key, THEMES["dark"])
```

- [ ] **Step 2: 验证主题定义**

```bash
python -c "
from theme import get_theme, THEMES
assert len(THEMES) == 3
assert get_theme('dark').name == '暗夜'
assert get_theme('light').name == '明亮'
assert get_theme('glass').name == '毛玻璃'
assert get_theme('unknown').name == '暗夜'
print('主题定义测试通过')
"
```

- [ ] **Step 3: 提交**

```bash
git add theme.py
git commit -m "feat: 添加三套主题定义"
```

---

### Task 4: 桌面显示面板 overlay.py

**Files:**
- Create: `overlay.py`

- [ ] **Step 1: 编写 OverlayPanel**

```python
"""桌面显示面板：右侧 overlay，鼠标穿透，置顶."""
from __future__ import annotations

from PyQt5.QtCore import Qt, QTimer, pyqtSignal
from PyQt5.QtWidgets import QWidget, QVBoxLayout, QLabel, QApplication
from PyQt5.QtGui import QFont

from data import DataStore, CATEGORY_LABELS
from theme import get_theme, CATEGORY_COLORS


class OverlayPanel(QWidget):
    """桌面右侧任务显示面板."""

    refresh_requested = pyqtSignal()

    def __init__(self, store: DataStore):
        super().__init__()
        self._store = store
        self._init_ui()
        self._apply_theme()
        self._position()
        self.refresh_requested.connect(self._refresh)
        self.refresh()

    def _init_ui(self):
        self.setWindowFlags(
            Qt.WindowStaysOnTopHint
            | Qt.FramelessWindowHint
            | Qt.Tool
        )
        self.setAttribute(Qt.WA_TransparentForMouseEvents, True)
        self.setAttribute(Qt.WA_TranslucentBackground, True)

        self._layout = QVBoxLayout(self)
        self._layout.setContentsMargins(16, 20, 16, 20)
        self._layout.setSpacing(6)

    def _position(self):
        screen = QApplication.primaryScreen().availableGeometry()
        panel_width = 260
        panel_height = screen.height() // 2
        x = screen.right() - panel_width - 10
        y = screen.top() + (screen.height() - panel_height) // 2
        self.setGeometry(x, y, panel_width, panel_height)

    def _refresh(self):
        """清空并重新渲染任务列表."""
        # 清除旧 widgets
        while self._layout.count():
            item = self._layout.takeAt(0)
            if item.widget():
                item.widget().deleteLater()

        theme = get_theme(self._store.theme)
        font = QFont("Microsoft YaHei", 11)

        tasks = self._store.get_active_tasks()
        if not tasks:
            # 无任务时显示提示
            hint = QLabel("暂无进行中的任务")
            hint.setFont(font)
            hint.setStyleSheet(f"color: {theme.text_primary}; opacity: 0.5;")
            hint.setAlignment(Qt.AlignCenter)
            self._layout.addWidget(hint)
        else:
            for task in tasks:
                row = QLabel(f"●  {task.text}")
                row.setFont(font)
                color = CATEGORY_COLORS.get(task.category, theme.text_primary)
                row.setStyleSheet(f"color: {color};")
                self._layout.addWidget(row)

        self._layout.addStretch()

    def _apply_theme(self):
        theme = get_theme(self._store.theme)
        self.setStyleSheet(f"background: {theme.overlay_bg}; border-radius: 12px;")

    def refresh(self):
        self._apply_theme()
        self._refresh()
```

- [ ] **Step 2: 验证 overlay 可创建（不显示）**

```bash
python -c "
import sys, os, tempfile, json
from PyQt5.QtWidgets import QApplication

app = QApplication(sys.argv)

# 准备测试数据
tmp = os.path.join(tempfile.gettempdir(), 'test_overlay.json')
with open(tmp, 'w', encoding='utf-8') as f:
    json.dump({'version': 1, 'theme': 'dark', 'tasks': [
        {'id': 'a', 'text': '任务一', 'category': 'now', 'done': False, 'sort': 0},
        {'id': 'b', 'text': '任务二', 'category': 'fire', 'done': False, 'sort': 1},
        {'id': 'c', 'text': '已完成', 'category': 'done', 'done': True, 'sort': 2},
    ]}, f)

from data import DataStore
from overlay import OverlayPanel

store = DataStore(tmp)
panel = OverlayPanel(store)
assert len(store.get_active_tasks()) == 2  # 已完成的不显示
panel.close()
os.remove(tmp)
print('OverlayPanel 测试通过')
"
```

- [ ] **Step 3: 提交**

```bash
git add overlay.py
git commit -m "feat: 添加桌面显示面板 overlay"
```

---

### Task 5: 编辑主界面 editor.py（上）—— 基础列表

**Files:**
- Create: `editor.py`

- [ ] **Step 1: 编写 EditorWindow 基础框架（输入栏 + 列表 + 复选框）**

```python
"""编辑主界面：任务列表、拖拽排序、分类管理."""
from __future__ import annotations

from PyQt5.QtCore import Qt, pyqtSignal
from PyQt5.QtWidgets import (
    QMainWindow, QWidget, QVBoxLayout, QHBoxLayout,
    QLineEdit, QComboBox, QPushButton, QListWidget, QListWidgetItem,
    QCheckBox, QLabel, QMenu, QAction, QApplication,
)
from PyQt5.QtGui import QFont, QColor, QBrush

from data import DataStore, Task, CATEGORIES, CATEGORY_LABELS
from theme import get_theme, CATEGORY_COLORS


class EditorWindow(QMainWindow):
    """编辑主界面."""

    data_changed = pyqtSignal()

    def __init__(self, store: DataStore):
        super().__init__()
        self._store = store
        self._init_ui()
        self._apply_theme()
        self.load_tasks()

    def _init_ui(self):
        self.setWindowTitle("工作备忘录")
        self.resize(420, 560)
        self.setMinimumSize(340, 400)

        central = QWidget()
        self.setCentralWidget(central)
        layout = QVBoxLayout(central)
        layout.setContentsMargins(12, 12, 12, 12)
        layout.setSpacing(8)

        # 输入栏
        input_layout = QHBoxLayout()
        self._input = QLineEdit()
        self._input.setPlaceholderText("输入新任务...")
        self._input.returnPressed.connect(self._add_task)

        self._category_combo = QComboBox()
        for cat in CATEGORIES:
            self._category_combo.addItem(CATEGORY_LABELS[cat], cat)

        self._add_btn = QPushButton("+")
        self._add_btn.setFixedWidth(32)
        self._add_btn.clicked.connect(self._add_task)

        input_layout.addWidget(self._input)
        input_layout.addWidget(self._category_combo)
        input_layout.addWidget(self._add_btn)
        layout.addLayout(input_layout)

        # 任务列表
        self._list = QListWidget()
        self._list.setDragDropMode(self._list.InternalMove)
        self._list.setDefaultDropAction(Qt.MoveAction)
        self._list.model().rowsMoved.connect(self._on_reorder)
        self._list.setContextMenuPolicy(Qt.CustomContextMenu)
        self._list.customContextMenuRequested.connect(self._show_context_menu)
        layout.addWidget(self._list)

    def _add_task(self):
        text = self._input.text().strip()
        if not text:
            return
        category = self._category_combo.currentData()
        self._store.add_task(text, category)
        self._input.clear()
        self.load_tasks()
        self.data_changed.emit()

    def load_tasks(self):
        """从 store 重新加载任务列表."""
        self._list.clear()
        tasks = sorted(self._store.tasks, key=lambda t: t.sort)
        for task in tasks:
            self._add_task_row(task)

    def _add_task_row(self, task: Task):
        """向列表添加一行任务."""
        item = QListWidgetItem()
        item.setData(Qt.UserRole, task.id)
        item.setFlags(
            Qt.ItemIsSelectable
            | Qt.ItemIsEnabled
            | Qt.ItemIsDragEnabled
            | Qt.ItemIsDropEnabled
        )

        row = _TaskRow(task, self._store)
        row.checkbox.stateChanged.connect(
            lambda state, tid=task.id: self._on_check(tid, state)
        )

        item.setSizeHint(row.sizeHint())
        self._list.addItem(item)
        self._list.setItemWidget(item, row)

    def _on_check(self, task_id: str, state: int):
        done = state == Qt.Checked
        self._store.update_task(task_id, done=done)
        self.load_tasks()
        self.data_changed.emit()

    def _on_reorder(self):
        """拖拽排序后重新编号."""
        task_ids = []
        for i in range(self._list.count()):
            item = self._list.item(i)
            task_ids.append(item.data(Qt.UserRole))
        self._store.reorder(task_ids)
        self.data_changed.emit()

    def _show_context_menu(self, pos):
        item = self._list.itemAt(pos)
        if not item:
            return
        task_id = item.data(Qt.UserRole)
        task = next((t for t in self._store.tasks if t.id == task_id), None)
        if not task:
            return

        menu = QMenu(self)

        # 切换分类子菜单
        cat_menu = menu.addMenu("切换分类")
        for cat in CATEGORIES:
            if cat != task.category:
                action = cat_menu.addAction(CATEGORY_LABELS[cat])
                action.triggered.connect(
                    lambda checked, tid=task_id, c=cat: self._change_category(tid, c)
                )

        menu.addSeparator()
        del_action = menu.addAction("删除")
        del_action.triggered.connect(
            lambda: self._delete_task(task_id)
        )

        menu.exec_(self._list.viewport().mapToGlobal(pos))

    def _change_category(self, task_id: str, category: str):
        self._store.update_task(task_id, category=category)
        self.load_tasks()
        self.data_changed.emit()

    def _delete_task(self, task_id: str):
        self._store.delete_task(task_id)
        self.load_tasks()
        self.data_changed.emit()

    def _apply_theme(self):
        theme = get_theme(self._store.theme)
        self.setStyleSheet(f"""
            QMainWindow {{
                background-color: {theme.editor_bg};
                color: {theme.editor_text};
            }}
            QLineEdit {{
                background-color: {theme.input_bg};
                color: {theme.input_text};
                border: 1px solid {theme.editor_border};
                border-radius: 4px;
                padding: 5px 8px;
            }}
            QComboBox {{
                background-color: {theme.input_bg};
                color: {theme.input_text};
                border: 1px solid {theme.editor_border};
                border-radius: 4px;
                padding: 4px 8px;
            }}
            QPushButton {{
                background-color: #0e639c;
                color: white;
                border: none;
                border-radius: 4px;
                font-weight: bold;
                font-size: 14px;
            }}
            QPushButton:hover {{
                background-color: #1177bb;
            }}
            QListWidget {{
                background-color: transparent;
                border: none;
            }}
        """)

    def closeEvent(self, event):
        """关闭窗口 → 最小化到托盘."""
        self.hide()
        event.ignore()

    def keyPressEvent(self, event):
        if event.key() == Qt.Key_Escape:
            self.hide()
        elif event.key() == Qt.Key_Delete:
            item = self._list.currentItem()
            if item:
                self._delete_task(item.data(Qt.UserRole))
        else:
            super().keyPressEvent(event)


class _TaskRow(QWidget):
    """单行任务控件：拖拽手柄 + 圆点 + 复选框 + 文本."""

    def __init__(self, task: Task, store: DataStore):
        super().__init__()
        self.task_id = task.id
        self._store = store
        self.checkbox = None
        self._init_ui(task, store.theme)

    def _init_ui(self, task: Task, theme_key: str):
        layout = QHBoxLayout(self)
        layout.setContentsMargins(4, 4, 4, 4)
        layout.setSpacing(8)

        theme = get_theme(theme_key)
        color = CATEGORY_COLORS.get(task.category, theme.text_primary)

        # 拖拽手柄
        handle = QLabel("☰")
        handle.setStyleSheet(f"color: {theme.editor_text}; opacity: 0.5; font-size: 12px;")
        handle.setFixedWidth(16)
        layout.addWidget(handle)

        # 分类圆点
        dot = QLabel("●")
        dot.setStyleSheet(f"color: {color}; font-size: 10px;")
        dot.setFixedWidth(14)
        layout.addWidget(dot)

        # 复选框
        self.checkbox = QCheckBox()
        self.checkbox.setChecked(task.done)
        self.checkbox.setStyleSheet(f"""
            QCheckBox::indicator {{
                width: 14px; height: 14px;
                border: 1px solid {theme.editor_border};
                border-radius: 3px;
                background: {theme.input_bg};
            }}
            QCheckBox::indicator:checked {{
                background: #0e639c;
                border-color: #0e639c;
            }}
        """)
        layout.addWidget(self.checkbox)

        # 任务文本
        text_label = QLabel(task.text)
        if task.done:
            text_label.setStyleSheet(
                f"color: {theme.editor_text};"
                "text-decoration: line-through;"
                "opacity: 0.5;"
            )
        else:
            text_label.setStyleSheet(f"color: {theme.editor_text};")
        layout.addWidget(text_label, stretch=1)

        # 分类标签
        cat_label = QLabel(CATEGORY_LABELS.get(task.category, ""))
        cat_label.setStyleSheet(f"""
            color: {color};
            font-size: 10px;
            background: transparent;
            border: 1px solid {color};
            border-radius: 8px;
            padding: 1px 8px;
        """)
        layout.addWidget(cat_label)
```

- [ ] **Step 2: 验证编辑器可创建**

```bash
python -c "
import sys, os, tempfile, json
from PyQt5.QtWidgets import QApplication

app = QApplication(sys.argv)

tmp = os.path.join(tempfile.gettempdir(), 'test_editor.json')
with open(tmp, 'w', encoding='utf-8') as f:
    json.dump({'version': 1, 'theme': 'dark', 'tasks': []}, f)

from data import DataStore
from editor import EditorWindow

store = DataStore(tmp)
w = EditorWindow(store)
w.load_tasks()
assert w._list.count() == 0

store.add_task('测试', 'now')
w.load_tasks()
assert w._list.count() == 1
w.close()
os.remove(tmp)
print('EditorWindow 基础测试通过')
"
```

- [ ] **Step 3: 提交**

```bash
git add editor.py
git commit -m "feat: 添加编辑主界面基础框架"
```

---

### Task 6: 编辑主界面 editor.py（下）—— 双击编辑和主题适配

**Files:**
- Modify: `editor.py`

- [ ] **Step 1: 在 `_TaskRow` 中添加双击编辑功能**

在 `_TaskRow._init_ui` 末尾（`layout.addWidget(cat_label)` 之后）追加：

```python
        # 双击编辑
        text_label.setCursor(Qt.IBeamCursor)
        text_label.mouseDoubleClickEvent = lambda e: self._start_edit(text_label, task)
```

在 `_TaskRow` 类末尾添加：

```python
    def _start_edit(self, label: QLabel, task: Task):
        theme = get_theme(self._store.theme)
        edit = QLineEdit(task.text)
        edit.setStyleSheet(f"""
            background: {theme.input_bg};
            color: {theme.input_text};
            border: 1px solid #0e639c;
            border-radius: 2px;
        """)
        edit.selectAll()
        # 替换 label
        idx = self.layout().indexOf(label)
        self.layout().removeWidget(label)
        label.hide()
        self.layout().insertWidget(idx, edit)
        edit.setFocus()

        def finish_edit():
            new_text = edit.text().strip()
            if new_text and new_text != task.text:
                self._store.update_task(task.id, text=new_text)
                # 通过信号通知刷新
                editor = self.window()
                if isinstance(editor, EditorWindow):
                    editor.load_tasks()
                    editor.data_changed.emit()

        edit.returnPressed.connect(finish_edit)
```

- [ ] **Step 2: 验证双击编辑**

```bash
python -c "
import sys, os, tempfile, json
from PyQt5.QtWidgets import QApplication

app = QApplication(sys.argv)
tmp = os.path.join(tempfile.gettempdir(), 'test_edit2.json')
with open(tmp, 'w', encoding='utf-8') as f:
    json.dump({'version': 1, 'theme': 'dark', 'tasks': [
        {'id': 'a', 'text': '原始文本', 'category': 'now', 'done': False, 'sort': 0}
    ]}, f)

from data import DataStore
from editor import EditorWindow, _TaskRow

store = DataStore(tmp)
w = EditorWindow(store)

# 验证 _TaskRow 存在且包含正确文本
row = w._list.itemWidget(w._list.item(0))
assert isinstance(row, _TaskRow)
print('双击编辑结构验证通过')

w.close()
os.remove(tmp)
"
```

- [ ] **Step 3: 提交**

```bash
git add editor.py
git commit -m "feat: 添加双击编辑和主题刷新"
```

---

### Task 7: 应用主控 app.py

**Files:**
- Create: `app.py`

- [ ] **Step 1: 编写 App 主控类**

```python
"""应用主控：托盘图标、窗口管理、快捷键."""
from __future__ import annotations

import sys
import os

from PyQt5.QtCore import Qt, QTimer
from PyQt5.QtWidgets import QApplication, QSystemTrayIcon, QMenu, QAction
from PyQt5.QtGui import QIcon, QPixmap, QPainter, QColor, QFont, QKeySequence
from PyQt5.QtWidgets import QShortcut

from data import DataStore
from theme import THEMES, get_theme
from overlay import OverlayPanel
from editor import EditorWindow


def _create_tray_icon_pixmap():
    """生成托盘图标（红色底 + 白色'备'字）."""
    pix = QPixmap(64, 64)
    pix.fill(Qt.transparent)
    painter = QPainter(pix)
    painter.setBrush(QColor(255, 71, 87))
    painter.setPen(Qt.NoPen)
    painter.drawRoundedRect(0, 0, 64, 64, 12, 12)
    painter.setPen(QColor(255, 255, 255))
    font = QFont("Microsoft YaHei", 36, QFont.Bold)
    painter.setFont(font)
    painter.drawText(pix.rect(), Qt.AlignCenter, "备")
    painter.end()
    return pix


class App:
    """应用主控."""

    def __init__(self):
        self._store = DataStore()
        self._overlay = OverlayPanel(self._store)
        self._editor = EditorWindow(self._store)

        # 数据变更 → 刷新 overlay
        self._editor.data_changed.connect(self._overlay.refresh)

        self._tray = QSystemTrayIcon()
        self._tray.setIcon(QIcon(_create_tray_icon_pixmap()))
        self._tray.setToolTip("工作备忘录")
        self._tray.activated.connect(self._on_tray_activated)
        self._build_tray_menu()
        self._tray.show()

        # 默认显示 overlay，隐藏编辑器
        self._overlay.show()

        # 注册全局快捷键 Ctrl+Alt+Q 退出
        self._hotkey = QShortcut(QKeySequence("Ctrl+Alt+Q"), self._editor)
        self._hotkey.activated.connect(self._quit)

    def _build_tray_menu(self):
        menu = QMenu()

        show_action = menu.addAction("显示编辑")
        show_action.triggered.connect(self._editor.show)

        theme_menu = menu.addMenu("主题")
        for key in THEMES:
            action = theme_menu.addAction(THEMES[key].name)
            action.triggered.connect(
                lambda checked, k=key: self._switch_theme(k)
            )

        menu.addSeparator()
        quit_action = menu.addAction("退出")
        quit_action.triggered.connect(self._quit)

        self._tray.setContextMenu(menu)

    def _on_tray_activated(self, reason):
        if reason == QSystemTrayIcon.DoubleClick:
            self._editor.show()
            self._editor.activateWindow()

    def _switch_theme(self, key: str):
        self._store.theme = key
        self._editor._apply_theme()
        self._editor.load_tasks()
        self._overlay.refresh()

    def _quit(self):
        self._tray.hide()
        self._editor.close()
        self._overlay.close()
        QApplication.quit()
```

- [ ] **Step 2: 验证 App 可实例化**

```bash
python -c "
import sys, os
from PyQt5.QtWidgets import QApplication

app = QApplication(sys.argv)
from app import App
a = App()
assert a._overlay.isVisible() is True
assert a._editor.isVisible() is False
a._quit()
print('App 主控测试通过')
"
```

- [ ] **Step 3: 提交**

```bash
git add app.py
git commit -m "feat: 添加应用主控托盘和窗口管理"
```

---

### Task 8: 入口文件 main.py

**Files:**
- Create: `main.py`

- [ ] **Step 1: 编写入口文件**

```python
"""桌面备忘录 v2 入口."""
import sys
import os

# 确保工作目录正确
os.chdir(os.path.dirname(os.path.abspath(__file__)))

from PyQt5.QtWidgets import QApplication
from app import App


def main():
    app = QApplication(sys.argv)
    app.setQuitOnLastWindowClosed(False)  # 关闭窗口不退出应用
    _ = App()
    sys.exit(app.exec_())


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 提交**

```bash
git add main.py
git commit -m "feat: 添加程序入口 main.py"
```

---

### Task 9: 集成验证和最终调整

**Files:**
- 无新建

- [ ] **Step 1: 启动程序验证所有功能**

```bash
# 在另一个终端启动程序
python main.py &
PID=$!
sleep 3

# 检查进程存在
kill -0 $PID 2>/dev/null && echo "程序运行正常" || echo "程序启动失败"

# 清理
kill $PID 2>/dev/null
```

**手动验证清单：**
1. 桌面右侧出现 overlay，鼠标可以穿透点击后面的图标
2. 双击托盘图标，编辑窗口弹出
3. 输入任务、选择分类、点 + 添加，overlay 同步刷新
4. 勾选复选框，任务从 overlay 消失
5. 拖拽手柄排序
6. 右键切换分类
7. 切换三套主题
8. 关闭编辑窗口 → 最小化到托盘，程序不退出

- [ ] **Step 2: 确认旧版文件保留**

```bash
ls -la 桌面备忘.py && echo "旧版文件已保留"
```

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "chore: 集成验证完成"
```

---

## 验证检查清单

实现完成后逐项确认：

- [ ] overlay 鼠标穿透（点击后面桌面图标不触发程序）
- [ ] overlay 始终置顶（不被其他窗口遮挡）
- [ ] 添加任务后 overlay 即时刷新
- [ ] 勾选完成后任务从 overlay 消失，编辑器中显示删除线
- [ ] 拖拽排序保存并重启后仍正确
- [ ] 三套主题切换正常，样式生效
- [ ] Esc 隐藏编辑窗口
- [ ] 关闭按钮最小化到托盘
- [ ] 双击托盘唤醒编辑窗口
- [ ] Ctrl+Alt+Q / 托盘退出 完全关闭程序
- [ ] 编辑 JSON 文件后重启，数据完整保留
